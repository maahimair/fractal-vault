# backend-python/requirements.txt
# Fractal Vault — Python Backend Dependencies
#
# Install:  pip install -r requirements.txt
# Python:   3.10+

# ── Core web framework ────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.7.0

# ── Configuration & Environment ───────────────────────────────
python-dotenv==1.0.1

# ── ML / numerics ─────────────────────────────────────────────
scikit-learn==1.5.1
numpy==1.26.4
joblib==1.4.2

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.2

# ── Removed (unused in current codebase) ──────────────────────
# pandas        — no DataFrame usage anywhere
# websockets    — WebSocket handled by Node gateway / socket.io
# transformers  — not yet integrated
# torch         — not yet integrated (~2 GB, do not add until needed)
# web3          — not yet integrated
# flwr          — not yet integrated
